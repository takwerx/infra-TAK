.venv/
.config/
CoreConfig.ldap-patch.xml
uploads/
ignite/
__pycache__/
*.pyc
.DS_Store

# CloudTAK plugins — plugins get their own repos when ready for GitHub.
# rapidsos: local-only, not yet tracked.
# takcad: tracked on dev for internal testing (TAK-CAD server not public yet).
cloudtak-plugins/rapidsos/
# Screenshots — no longer tracked (attach images inline via the Claude extension).
screenshots/

# TAK-CAD server-plugin reference docs — LOCAL ONLY, never pushed to GitHub.
# (TAK-CAD server plugin is closed-source; specs live here for Claude to review.)
tak-cad-docs/

# Internal working notes + dev tooling moved to the PRIVATE repo
# takwerx/infra-TAK-notes. This repo is public — keep these out of it.
# Open ~/GitHub/infra-TAK.code-workspace to edit both repos together.
# docs/ contents are ignored by default (private-by-default), with an explicit allow-list
# for product-safe docs that are intentionally public (scrub before adding a new one).
docs/*
!docs/GUARDDOG.md
!docs/AUTHENTIK-LOGIN-BRANDING.md
!docs/RELAY-SETUP.md
!docs/DDNS-SETUP.md
memory-bank/
.cursor/
.cursorrules
ops/
STATUS.md
TESTING.md
ssdnodes-firewall-request.md
pull-dev-and-restart.sh

# Local operator credentials and secrets — never commit
.local/
.local/eud-remote-assist-upstream-handoff.md

# Claude Code project guidance — local only, full copy mirrored in private infra-TAK-notes
CLAUDE.md

# Claude Code project skills/hooks — local only, mirrored in private infra-TAK-notes
.claude/
