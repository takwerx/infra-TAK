# infra-TAK console Python dependencies — pinned for fleet uniformity.
# Every box installs these exact versions regardless of install date.
# Top-level pins only (no transitive freeze) — keeps apt/dnf families and
# Python 3.9 (RHEL/Rocky 9) / 3.10 (Ubuntu 22.04) compatibility simple.
# Bump pins deliberately here; never by re-running start.sh with loose specs.
#
# gunicorn is held at 23.0.0: the newest release supporting Python 3.9
# (RHEL/Rocky 9 default python3). gunicorn >=24 requires Python >=3.10
# and would break the RHEL family.
flask==3.1.3
werkzeug==3.1.8
gunicorn==23.0.0
psutil==7.2.2
pyyaml==6.0.3
pystun3==2.0.0
